
# Dependencies
node_modules/
package-lock.json

# Build outputs (generated by build.js)
posts/

# (Note: blog.html is also generated, but we can leave it tracked or untracked based on preference. We'll leave it tracked so GitHub Pages could work if used instead of Vercel.)
